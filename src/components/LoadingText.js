import React from 'react'
import { Typography } from '@mui/material'

const LoadingText = () => {
  return (
    <Typography variant="h6"  sx={{textAlign:"center",marginTop:"10%", fontWeight: 500 }}>Loading...</Typography>
  )
}

export default LoadingText